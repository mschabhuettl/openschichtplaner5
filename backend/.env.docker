# ==============================================================================
# OpenSchichtplaner5 — Docker-spezifische Konfiguration
# Wird von docker-compose.yml via env_file eingebunden.
# Für lokale Entwicklung außerhalb Docker: backend/.env verwenden.
# ==============================================================================

# Datenbank liegt im benannten Docker-Volume sp5_data
SP5_DB_PATH=/app/sp5_db/Daten

# In Produktion UNBEDINGT ändern: openssl rand -hex 32
SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32

# Nur der eigene Container-Port ist erlaubt (Nginx/Reverse-Proxy davor schalten)
ALLOWED_ORIGINS=http://localhost:8000

HOST=0.0.0.0
PORT=8000
DEBUG=false
TOKEN_EXPIRE_HOURS=8

# Rate Limiting
RATE_LIMIT_LOGIN=5/minute
RATE_LIMIT_API=200/minute
BRUTE_FORCE_MAX_ATTEMPTS=5
BRUTE_FORCE_LOCKOUT_MINUTES=15

# Logging — in Docker nach /app/logs (Volume oder gemountetes Verzeichnis)
LOG_LEVEL=INFO
LOG_FILE=/app/logs/sp5-api.log

# Session
SESSION_CLEANUP_INTERVAL_MINUTES=60

# Sicherheits-Features
# HSTS nur aktivieren wenn HTTPS via Reverse-Proxy (nginx/caddy) davor liegt
SP5_HSTS=false
# Dev-Modus NIEMALS in Produktion! Erlaubt Auth-Bypass.
SP5_DEV_MODE=false

# App
APP_NAME=OpenSchichtplaner5
APP_VERSION=1.0.0
