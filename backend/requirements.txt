# REST API layer (FastAPI app, routers, auth/2FA, reports/exports), maintained in
# its own repo and released to PyPI. Importable as `sp5api`. Extracted from
# backend/api with full git history. Pulls in fastapi/uvicorn/pydantic etc. as
# its own dependencies.
openschichtplaner5-api>=1.2.0

# Core library (DBF bridge + ORM/sync), maintained in its own repo and released to
# PyPI. Importable as `sp5lib`. Kept as a direct dependency: scripts/ and
# backend/alembic/env.py import it without going through the API package.
libopenschichtplaner5[postgres]>=1.7.0

# Used directly by scripts/seed_demo_data.py
python-dotenv>=1.2.2

# Lint (make lint runs ruff from backend/.venv)
ruff>=0.1.0
