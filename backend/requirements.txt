# 0.136.3 is a malicious release (MAL-2026-4750): it tampers with
# pyproject.toml/PKG-INFO to inject an undocumented 'fastar' dependency. Exclude it.
fastapi>=0.104.0,!=0.136.3
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
httpx>=0.24.0
starlette>=0.27.0
anyio>=3.7.0
python-multipart>=0.0.7
python-dotenv>=1.0.0
ruff>=0.1.0
slowapi>=0.1.9
fpdf2>=2.7.0
bcrypt>=4.0.0
PyJWT>=2.8.0
pyotp>=2.9.0
qrcode[pil]>=7.4
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
openpyxl>=3.1.0

# Core library (DBF bridge + ORM/sync), maintained in its own repo and released to
# PyPI. Importable as `sp5lib`. The [postgres] extra pulls psycopg2-binary.
# 1.2.0 adds the Shift/LeaveType/Workplace ORM models + repositories; 1.3.0 adds
# the schedule-entry models (ShiftAssignment/SpecialShift/Absence) with date-range
# repositories and makes sync_groups dangling-FK tolerant, so sync_all is now safe
# on dirty data — all consumed by the ORM-mirror admin router.
libopenschichtplaner5[postgres]>=1.3.0

pytest>=7.4.0
pytest-cov>=4.1.0
psutil>=5.9.0
Pillow>=10.0.0
